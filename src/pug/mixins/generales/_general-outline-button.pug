mixin general-outline-button(textContent, iconClass = '')
    button.general-outline-button&attributes(attributes)
        if iconClass
            span(class=`icon-${iconClass}`)
        span #{textContent}
