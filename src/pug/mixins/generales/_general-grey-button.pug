mixin general-grey-button(textContent, iconClass = '')
    button.general-grey-button&attributes(attributes)
        if iconClass
            span(class=`icon-${iconClass}`)
        span #{textContent}
