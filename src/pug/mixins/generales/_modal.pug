mixin modal(id)
    div(id=`modal-${id}`).modal
        .modal__overlay.modal__overlay--hidden(id=`modal-${id}-overlay`)
        .modal__container.modal__container--hidden(id=`modal-${id}-contents`)
            block